# SeatAllocationSystem
The goal is to build a Command Line Interface (CLI) tool that manages seats dynamically. The system will track "Idle Time" (how long a student has been away). If a student stays away longer than a set limit (e.g., 15 minutes), the system automatically cancels their booking and gives the seat to the next person waiting.
